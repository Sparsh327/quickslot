services:
  - type: web
    name: quickslot-api
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run db:generate && npm run build && npx prisma db push --accept-data-loss && npm run db:seed
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
